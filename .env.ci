ENV=ci
APP_TITLE="Template Project"
APP_NAME=git_notification

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=test
POSTGRES_PASSWORD=test
POSTGRES_DB=test

SENTRY_DSN=
PROMETHEUS_METRICS_KEY=key

# Deterministic secrets used by the webhook tests.
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=test-telegram-secret
GITLAB_WEBHOOK_SECRET=test-gitlab-secret
